[pytest]
asyncio_mode = auto
testpaths = tests
# Put the parent of the backend/ package on sys.path so `import backend.X` resolves.
# When running from betwise-casino/backend/, ".." is betwise-casino/ which contains
# the backend/ directory — i.e. `import backend.game.strategy` works.
pythonpath = ..
    .
