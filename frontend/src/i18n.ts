/**
 * i18n.ts — minimal t() helper for future internationalization.
 * Currently a passthrough; replace with i18next or similar when ready.
 */
export function t(key: string): string {
  return key;
}
